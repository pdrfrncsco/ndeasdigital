# API Backend URL (sem barra final)
# IMPORTANTE: Configure esta variável antes do deploy
# Exemplo: NEXT_PUBLIC_API_BASE=https://api-ndeas.ndeas.cloud

NEXT_PUBLIC_API_BASE=https://api-ndeas.ndeas.cloud

# Ambiente
NODE_ENV=production