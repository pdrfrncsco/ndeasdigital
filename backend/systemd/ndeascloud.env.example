# Example environment file for ndeascloud (place at /var/www/ndeascloud/backend/.env)
# Replace placeholder values with real credentials in production.

# Django secret
DJANGO_SECRET=tsjofm(%76_29@axa978mr(6vi5zn=b7zrgk%&5#t@n#)-1l05

# Debug mode (False in production)
DJANGO_DEBUG=False

# Email / SMTP settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=ndeasdigital@gmail.com
EMAIL_HOST_PASSWORD=E.Ndeas@25
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=no-reply@ndeas.cloud

# Optional: comma-separated fallback recipients (admins)
EMAIL_FALLBACK_RECIPIENTS=ndeasdigital@gmail.com,pdrfrncsco@gmail.com

# Retry tuning
EMAIL_SEND_MAX_ATTEMPTS=3
EMAIL_SEND_BASE_DELAY=1.0

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=ndeas.cloud
